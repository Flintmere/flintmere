# Local task state, ephemeral agent output
.agents/
.continue/
MEMORY.md

# OS / editor crud
.DS_Store
*.swp
.idea/
.vscode/

# Don't ship personal project context with the kit. Each consumer
# project populates its own projects/<name>/ via init.sh.
context/

# Dist / build outputs that might get generated by skills' companion scripts
dist/
build/
node_modules/
